Metadata-Version: 2.4
Name: cortex-coach
Version: 0.1.0
Summary: Standalone runtime CLI for Cortex lifecycle coaching.
Requires-Python: >=3.10
Description-Content-Type: text/markdown
Requires-Dist: jsonschema>=4.10.0

# cortex-coach

Standalone runtime CLI for Cortex lifecycle coaching.

## Install (dev)

```bash
uv sync --group dev
```

## Run

```bash
uv run cortex-coach --help
```

Fallback script mode:

```bash
uv run python3 cortex_coach/coach.py --help
```

## Quality Gate

```bash
./scripts/quality_gate_ci_v0.sh
```
